import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#369D72", // Cor principal do tema
    },
    secondary: {
      main: "#BCDCB1", // Cor secundária do tema
    },
  },
});
